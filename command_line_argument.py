# !/usr/bin/env python3
# run this program with ubuntu shell with ./command_line_argument with some argument like this
# ./command_line_agrument.py one two three
# import sys
# print(sys.argv)


# run this program with ubuntu shell with ./command_line_argument with an argument like this
# ./command_line_agrument.py filename

# import os
# import sys
# filename = sys.argv[1]
# # create a new file if the file isn't exist in path variable
# if not os.path.exists(filename):
#     with open(filename, 'w') as f:
#         f.write("Writing new line")
# else:
#     print("Error, the file {} already exist".format(filename))
#     sys.exit(1)


